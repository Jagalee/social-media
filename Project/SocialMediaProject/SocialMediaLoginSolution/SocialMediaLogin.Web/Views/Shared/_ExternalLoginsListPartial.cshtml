@model ICollection<AuthenticationClientData>

@if (Model.Count == 0)
{
    <div class="message-info">
        <p>There are no external authentication services configured. See <a href="http://go.microsoft.com/fwlink/?LinkId=252166">this article</a>
        for details on setting up this ASP.NET application to support logging in via external services.</p>
    </div>
}
else
{
    using (Html.BeginForm("ExternalLogin", "Home", new { ReturnUrl = ViewBag.ReturnUrl }))
    {
    @*@Html.AntiForgeryToken()*@
    <span id="socialLoginList">

        @foreach (AuthenticationClientData p in Model)
        {
            @*<button type="submit" name="provider" value="@p.AuthenticationClient.ProviderName">@p.DisplayName</button>*@
            <input type="hidden" value="@p.AuthenticationClient.ProviderName" id="provider"/>
            <a id="click" href="#">@p.DisplayName</a>
        }

    </span>
    }
}

<script>
    $(document).ready(function () {
        $("#click").on("click", function () {
            var firstname = "abc";
            var username = "abcd";
            location.href = '@Url.Action("ExternalLogin", "Home")?provider=' + $("#provider").val() + '&returnUrl=' + null;
        });
    })
</script>